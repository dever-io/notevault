export function NotesPage() {
  return (
    <section className="nv-card">
      <h2>My notes</h2>
      <p className="nv-muted">
        Note CRUD UI is built across T02 (create), T03 (edit/delete), T06 (API wiring),
        and T07 (real-time search).
      </p>
    </section>
  );
}

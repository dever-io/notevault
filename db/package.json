{
  "name": "notevault-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NoteVault database — Postgres schema, migrations, and seed data.",
  "scripts": {
    "migrate": "node migrate.mjs",
    "seed": "psql \"$DATABASE_URL\" -f seed.sql"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}

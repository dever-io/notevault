{
  "name": "notevault-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NoteVault end-to-end tests — Playwright, runs against a local backend + frontend.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "pg": "^8.13.1",
    "@types/node": "^22.10.1"
  }
}
